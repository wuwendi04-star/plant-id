<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 相机权限（拍照记录植物成长） -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- NFC 标签读取权限 -->
    <uses-permission android:name="android.permission.NFC" />
    <!-- 推送通知权限（Android 13+ / API 33+） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 声明相机为可选特性（允许无相机的设备安装） -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <!-- 声明 NFC 为可选特性（允许无 NFC 的设备安装） -->
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Plantid">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Plantid">
            <!-- 应用启动入口 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- NFC: NDEF 格式标签 + URI plantid://nfc/<UID>（冷启动直跳档案的核心路径） -->
            <!-- 仅匹配已被本 App 写入 URI 记录的标签，防止任意 NFC 标签冷启动本 App -->
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    android:scheme="plantid"
                    android:host="nfc" />
            </intent-filter>
        </activity>

        <!-- FileProvider：允许相机 App 向 App 私有目录写入照片 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
    </application>

</manifest>